name: CI

on:
  push:
    branches:
      - main
      - "ship/**"
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Bootstrap
        run: script/bootstrap
      - name: Test
        run: script/cibuild
